# Craig and Landreth Cars -- a real dealership, for a booked demo.
#
# Everything here was read off their own pages: the header address block and
# the schema.org AutoDealer block agree, and the hours and copy come from their
# home page. Nothing is inferred, with one stated exception -- the brand accent
# below, which was read off a screenshot by eye because nothing here can reach
# their site.
#
# This one file is the whole dealership: who they are, when they open, what
# their storefront says, where their cars come from, and who works there.
# `DEALERSHIP=craigandlandreth` is the only line `.env` needs.

name: Craig and Landreth Cars

# The St. Matthews store. Their site lists three lots on one page; which one
# this instance is comes from `inventory.dealer_id` further down.
timezone: America/Kentucky/Louisville
address: 4156 Shelbyville Rd., Louisville, KY 40207
phone: 502-447-3450
website_url: "https://www.craigandlandrethcars.com/"

# From the "Our Hours" table on their own home page. Note Friday and Saturday
# close an hour earlier than Mon-Thu -- worth keeping exact rather than
# rounding to one range, because `check_availability` builds the slots it
# offers a buyer straight out of these values, and a 7:30 PM slot on a Friday
# is an appointment nobody is there for.
hours:
  monday:    { open: "09:00", close: "20:00" }
  tuesday:   { open: "09:00", close: "20:00" }
  wednesday: { open: "09:00", close: "20:00" }
  thursday:  { open: "09:00", close: "20:00" }
  friday:    { open: "09:00", close: "19:00" }
  saturday:  { open: "09:00", close: "19:00" }
  sunday:    null   # closed

# Their livery. Only the accent family moves -- every structural token still
# comes from the one theme layer, so their colour cannot restyle the product
# into something that no longer reads.
brand:
  # Read off the screenshot of their own home page by eye, not sampled -- the
  # egress proxy refuses their site and their image CDN, so nothing here could
  # fetch either. If it is off, this one line is the fix.
  accent: "#f26a21"
  accent_ink: "#ffffff"
  # Their site is orange on near-black, and an orange accent on a white page
  # looks nothing like it. Read ONLY by /showroom -- the storefront is theirs,
  # and their reps' dashboard is a working tool that should not change colour
  # because a prospect's marketing site is dark.
  surface: dark
  logo_url: "https://imagescdn.dealercarsearch.com/DealerImages/1123/20820/logow2.png"

# Their front page, as `/showroom` renders it. Every string below is theirs,
# copied from their own home page -- their headings, their sentences, their
# links. It lives here rather than in Showroom.tsx for the same reason their
# name does: a prospect's copy written into a component is the next instance
# greeting somebody as Craig and Landreth.
#
# The links point at their real site on purpose. A nav that 404s inside our
# app during a demo is worse than one that leaves it, and these are pages we
# are not pretending to have rebuilt.
site:
  tagline: Your Premier Car Dealership In Louisville, KY
  heading: Welcome To Craig and Landreth Cars
  hero_image: "https://imagescdn.dealercarsearch.com/DealerImages/1123/20820/50yrslide4.jpg"
  welcome:
    - >-
      Let Craig and Landreth Cars show you how easy it is to buy a quality used
      car in Louisville. We believe fair prices, superior service, and treating
      customers right leads to satisfied repeat buyers.
    - >-
      Our friendly and knowledgeable sales staff is here to help you find the
      car you deserve, priced to fit your budget. Shop our virtual showroom
      online then stop by for a test drive.
  links:
    - { label: Inventory,     href: "https://www.craigandlandrethcars.com/inventory" }
    - { label: We Buy Cars,   href: "https://www.craigandlandrethcars.com/webuycars" }
    - { label: Financing,     href: "https://www.craigandlandrethcars.com/financing" }
    - { label: Services,      href: "https://www.craigandlandrethcars.com/services" }
    - { label: Our Store,     href: "https://www.craigandlandrethcars.com/ourstore" }
  social:
    - { label: Facebook, href: "https://www.facebook.com/Craigandlandrethcars" }
    - { label: X,        href: "https://x.com/CnLcars" }
    - { label: YouTube,  href: "https://www.youtube.com/@CandLUsedCarsKY" }

# No `showroom_fixture` here, deliberately. Riverside's fourteen vehicles, its
# sample CSV lot and its populated yesterday are invented for a demo of a
# dealership that does not exist -- seeded here they would put a Toyota Sienna
# from Cedar Falls, Iowa in front of a Louisville buyer. Their lot comes from
# their own site, below.

# Where their cars come from. Facts about this dealership, so they live here
# rather than in `.env` -- switching DEALERSHIP= to another prospect and
# forgetting to change a SCRAPER_ line crawled Craig's site into somebody
# else's instance, silently, because a successful crawl of the wrong site
# looks exactly like a successful crawl.
#
# Press Import on /app/inventory, review the run, then publish.
inventory:
  # Their whole group, exported by hand and committed, because their site
  # refuses this crawler from every machine tried so far -- the TCP connection
  # never opens, so it is below the HTTP layer and no user agent changes it.
  # `make reset-db` rebuilds the real 486-car lot with no network at all.
  #
  # It goes through the same CSV importer a dealer's own upload does. Re-run
  # `make ingest ARGS=--publish` from a machine that can reach them and the
  # crawl takes over; the VINs are the same, so it is a diff rather than a
  # second lot.
  #
  # NOTE this is all three of their stores -- Louisville (240), Clarksville IN
  # (176) and Bullitt County (70) -- which is what their own site lists. Each
  # car carries its `location`, and a car at another store says so before
  # Liner offers a time, because the appointment is at the address in
  # `dealerships` and that is only one of the three.
  fixture_csv: "craigandlandreth/inventory.csv"
  source_url: "https://www.craigsbestcars.com/newandusedcars"
  # Their listing page mixes three stores -- Louisville (1123), Clarksville IN
  # (1129) and Bullitt County (3833) -- each card carrying its own id. This app
  # holds exactly one dealership, so without this Liner offers a buyer a car
  # that is a two-hour drive from the showroom it says it is standing in.
  dealer_id: "1123"

# Their people, so a reseed rebuilds them. `make add-user` adds somebody to a
# live box without one; this is the other half, so a prospect's own manager
# does not disappear the next time the fixture is rebuilt.
#
# Each gets a generated password, printed once by the seed. Only `manager` and
# `rep` -- our own accounts are in `ops_users` and never come from here.
# **Austin is real; the four below are placeholders.** They are here because a
# roster of one has nobody to hand a buyer to, so assignment, the per-rep
# queues and the team page have nothing to show -- and a demo that cannot show
# those is missing the half of the product a manager cares about.
#
# They are invented, and the addresses say so: `@placeholder.invalid` cannot
# resolve or receive mail, so nothing here can reach anyone, and a name that
# turns out to be wrong is visibly wrong rather than quietly plausible. Replace
# the four with their real people before this is in front of them -- invented
# colleagues on a page their own manager is reading is the failure this file
# otherwise exists to prevent -- and delete any that are left over.
staff:
  - { name: Austin, email: austin@craigandlandrethcars.com, role: manager }
  - { name: Sales Rep One, email: rep1@placeholder.invalid, role: rep }
  - { name: Sales Rep Two, email: rep2@placeholder.invalid, role: rep }
  - { name: Sales Rep Three, email: rep3@placeholder.invalid, role: rep }
  - { name: Sales Rep Four, email: rep4@placeholder.invalid, role: rep }

# ---------------------------------------------------------------------------
# What Liner knows beyond the listings.
#
# `answer_from_knowledge` returns the `answer` below **word for word**. It is
# never summarised, never rephrased and never composed -- a policy answer is
# one a buyer repeats back to a rep, so a plausible near-miss is worse than
# nothing. An unanswered question returns `found: false`, Liner says a
# colleague will confirm, and the buyer lands in Needs a person.
#
# So only what they have actually told us goes here. Riverside's $189 doc fee
# and $500 deposit are invented for a fixture, and seeding them would have
# Liner quote a real customer a policy nobody at this dealership has agreed to.
#
# HOW A QUESTION FINDS AN ENTRY (`lookup_knowledge`)
#   A word shared with the *topic* scores 3, a word shared with the *answer*
#   scores 1, and nothing under 3 matches at all. So the topic has to carry
#   the words a buyer would actually type -- "Doc fee" is matched by "what's
#   your doc fee?" on one word, where a topic called "Fees and charges" is
#   not. Common words ("do", "you", "have", "car") are stripped first, and a
#   trailing plural is trimmed, so "deposits" finds "Deposit".
#
# WRITING ONE
#   - Topic: two or three words, the ones a buyer says. Not a heading.
#   - Answer: one or two sentences, first person, no markdown -- it is spoken
#     on a call as well as printed in chat.
#   - State what is true and stop. An answer that hedges ("typically", "may
#     vary") is one the buyer has to ask a person about anyway.
#   - Where a number differs by store, say so in the one answer. Liner cannot
#     pick a branch-specific answer: the text is returned verbatim, and every
#     car already tells the buyer which lot it is on.
#
# TWO CHIPS DEPEND ON THIS LIST. "What's your doc fee?" and "Do you take
# trade-ins?" are only put on the buyer's screen when the matching topic
# exists -- a button promising an answer nobody wrote is a question the
# dealership is asking on its own behalf and cannot answer.
knowledge:
  # Confirmed by Austin. Their own export carries a per-car doc_fee that agrees
  # -- 690 on all 310 Louisville and Bullitt cars, 260 on Clarksville -- except
  # two Clarksville rows that say 245 (a 2022 VW Taos, MU20691, and a 2013
  # Toyota Tacoma, MU20862A). This entry is the authority and the per-car field
  # is not read by anything: one number a person stands behind beats 486 that
  # disagree twice. Worth asking them which is right.
  - topic: Doc fee
    answer: >-
      Our documentation fee is $690 at the Louisville and Bullitt County
      stores, and $260 at Clarksville. It's on the buyer's order before
      anything is signed.

# STILL TO ASK AUSTIN. Each of these is a question a buyer asks in the first
# five minutes, and today each one costs a rep a phone call. Uncomment and fill
# in -- do not guess, and leave out anything they have not answered.
#
# - topic: Trade-ins
#   answer: "..."        # Do you appraise on the spot? What should they bring?
# - topic: Deposits
#   answer: "..."        # Does a deposit hold a car? How much, refundable?
# - topic: Financing
#   answer: "..."        # Who do you finance through? Bad credit?
# - topic: Warranty
#   answer: "..."        # What comes with a car as standard?
# - topic: Test drives
#   answer: "..."        # Walk-in or booked? Licence and insurance needed?
# - topic: Vehicle history
#   answer: "..."        # CarFax free? Printed before signing?
# - topic: Out-of-state buyers
#   answer: "..."        # You are ten minutes from Indiana -- this one comes up.
# - topic: Payment methods
#   answer: "..."        # Cash limit? Personal cheques?
#
# Hours and the address are deliberately NOT here. They are on the dealership
# row and reach the prompt from `hours_json`; a second copy in this table is a
# second thing to change when they open later on a Saturday.
